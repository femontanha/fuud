import React from 'react';
import Playlist from '../components/Playlist';

const Home = () => (
    <div>
        <h1>Filter for Playlists</h1>
        <Playlist />
    </div>
);

export default Home;
